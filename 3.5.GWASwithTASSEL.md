# 5. GWAS

### TASSEL 5.0

We will now be downloading Tassel 5.0 to do additional QC steps.
```
git clone https://bitbucket.org/tasseladmin/tassel-5-standalone.git
```
```
cd tassel-5-standalone/
```
PCA ran on genotype data set
```
/2/scratch/yuying/SEQAF/bamfiles/tassel-5-standalone/run_pipeline.pl -fork1 -vcf pruned_2.vcf -PrincipalComponentsPlugin -covariance true -endPlugin -export PCAfile
```

```
/2/scratch/yuying/SEQAF/bamfiles/tassel-5-standalone/run_pipeline.pl -fork1 -vcf pruned_2.vcf -fork2 -PrincipalComponentsPlugin -covariance true -ncomponents 5 -input1 -endPlugin -fork3 -r AMBphe.txt -fork4 -KinshipPlugin –input1 -method Centered_IBS -endPlugin -combine5 -input1 -input2 input3 -intersect -combine6 –input5 -input4 -mlm -export amb_tassel_output
```

### Plink 1.90 beta

GLM so will do IBD and PCA

We will first analyse for cryptic relatedness
- we will exclude all individuals above the pihat threshold of 0.2
- 
 
plink --vcf ISA.snp.vcf --allow-extra-chr --pheno ISA.qt.txt  --allow-no-sex --recode --out Merge

plink --file Merge --allow-extra-chr --make-bed --allow-no-sex --out Merge

- - 521803 variants and 41 people pass filters and QC.
 

plink --bfile Merge --geno 0.02 --mind 0.02 --make-bed --out Merge_mind_geno --allow-extra-chr --allow-no-sex

- - 518500 variants and 41 people pass filters and QC.
 

plink --bfile Merge_mind_geno --indep-pairwise 50 5 0.2  --allow-extra-chr --allow-no-sex

- -Pruning complete.  453229 of 518500 variants removed. 65,271 left.
 

plink --bfile Merge_mind_geno --extract plink.prune.in --genome --min 0.2 --out pihat_min0.2 --allow-extra-chr --allow-no-sex

plink --bfile Merge_mind_geno --filter-founders --make-bed --out Merge_founders --allow-extra-chr --allow-no-sex

plink --bfile Merge_founders --remove 0.2_low_call_rate_pihat.txt --make-bed --out Merge_pihat --allow-extra-chr --allow-no-sex

plink --bfile Merge_pihat --allow-extra-chr --allow-no-sex --recode --out Merge_pihat

plink --bfile Merge_pihat --pheno  ISA.qt.txt --extract plink.prune.in --genome --out Merge_ready --allow-extra-chr --allow-no-sex

plink --bfile Merge_founders --extract plink.prune.in --genome --remove 0.2_low_call_rate_pihat.txt  --min 0.2 --make-bed --out prune_out --allow-extra-chr --allow-no-sex

- - 65271 variants and 36 people pass filters and QC.
 

plink --bfile prune_out --pca 20 --out prune_out.pca --allow-extra-chr --allow-no-sex

plink --bfile Merge_major_cluster --linear --pheno POS.txt --allow-no-sex --allow-extra-chr --covar PC5.txt --hide-covar --out 10PCA_linearres

 
