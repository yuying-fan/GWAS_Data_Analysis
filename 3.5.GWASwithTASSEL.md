# 5. GWAS

### TASSEL 5.0

We will not be downloading Tassel 5.0 to do additional QC steps.
```
git clone https://bitbucket.org/tasseladmin/tassel-5-standalone.git
```

PCA ran on genotype data set
```
./2/scratch/yuying/SEQAF/bamfiles/tassel-5-standalone/run_pipeline.pl -fork1 -vcf pruned_2.vcf -PrincipalComponentsPlugin -covariance true -endPlugin -export PCAfile
```

```
./run_pipeline.pl -vcf pruned_2.vcf -KinshipPlugin -method Centered_IBS -endPlugin -export kinshipfile
```

```
/2/scratch/yuying/SEQAF/bamfiles/tassel-5-standalone/run_pipeline.pl -fork1 -vcf pruned_2.vcf -fork2 -r AMBphenotype.txt -fork3 -importGuess PCAfile -combine4 -input1 -input2 -input3 -intersect -fork5 -importGuess kinshipfile -combine6 -input5 -input4 -mlm -mlmMaxP 0.01 -export MLM_Output
```
