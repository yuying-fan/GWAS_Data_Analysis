# 4. Quality Control and Prepping for Association Analysis

### Variant Filtering

For our first QC step, we will be filtering our SNPs. 
```
/usr/local/vcflib/bin/vcffilter -f "TYPE = snp & QUAL > 13 & DP > 20" mergedall.vcf > filter1.vcf
```

